# Copy to .env before a production Docker deployment.
# Use independent, long random values. Do not commit .env.
ANHEYU_DATABASE_PASSWORD=replace-with-a-long-random-database-password
ANHEYU_REDIS_PASSWORD=replace-with-a-long-random-redis-password
